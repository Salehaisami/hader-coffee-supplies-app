{
  "name": "hader-firestore-rules-tests",
  "version": "1.0.0",
  "description": "Firestore security rules unit tests for Hader app",
  "scripts": {
    "test": "jest --forceExit",
    "test:emulator": "firebase emulators:exec --only firestore 'npm test'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.0",
    "@types/jest": "^29.5.0",
    "firebase": "^10.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.4.0"
  }
}
